# 1.13 — Exercises and Applied Problems

**Workflow Stage:** Revised Final / Canonical Edition  
**Status:** CANONICAL — COMPLETE  
**Book:** Book 1 — Python Foundations for Financial Mathematics  
**Chapter:** Chapter 1 — Introduction to Python for Financial Mathematics

---

## 1.13.1 Purpose

This section provides exercises and applied problems covering the concepts developed throughout Chapter 1.

The exercises progress through five levels:

```text
Foundation
    │
    ▼
Application
    │
    ▼
Analysis
    │
    ▼
Debugging
    │
    ▼
Integration
```

The objective is to develop four related skills:

1. understanding foundational Python concepts,
2. translating financial mathematics into Python expressions,
3. handling decimal financial quantities carefully,
4. validating and interpreting calculated results.

For each problem, consider the financial definition, appropriate representation, mathematical expression, Python implementation, required validation, rounding stage, and independent verification.

Unless an exercise states otherwise, monetary examples use decimal quantities. Any rounding policy should be stated explicitly rather than assumed.

---

## Part I — Concept Review

### 1.13.2 Exercise 1 — Financial Mathematics

In your own words, explain **financial mathematics**. Distinguish among the financial problem, mathematical model, and computer implementation. Give two examples of financial questions that mathematics can help answer.

### 1.13.3 Exercise 2 — Why Use Python?

Identify at least five reasons Python can be useful when studying or implementing financial mathematics. For each reason, explain whether the benefit relates primarily to programming, mathematics, data, automation, reproducibility, financial analysis, or more than one category.

### 1.13.4 Exercise 3 — Related Financial Fields

Explain the distinction among financial mathematics, quantitative finance, and financial engineering. Then explain why these fields overlap without necessarily being identical.

### 1.13.5 Exercise 4 — Financial Variables

Suppose a financial calculation uses principal = $25,000, annual rate = 6%, and time = 4 years. Create descriptive Python variables representing the values. Explain why names such as `principal`, `annual_rate`, and `years` are generally more maintainable than `x`, `y`, and `z`.

---

## Part II — Python Data Representation

### 1.13.6 Exercise 5 — Identify the Data Type

Identify the likely Python type of each value:

```python
25000
25000.75
"25000.75"
True
Decimal("25000.75")
```

Explain which representations are appropriate for whole-number counters, textual user input, Boolean conditions, and decimal monetary examples.

### 1.13.7 Exercise 6 — Decimal Construction

Compare `Decimal("0.1")` with `Decimal(0.1)`. Explain why the expressions do not necessarily represent the same decimal value. Which construction should generally be preferred when the intended decimal literal is exactly `0.1`?

### 1.13.8 Exercise 7 — Financial Data

Create `Decimal` variables representing an account balance of $18,450.75, transaction amount of $925.40, fee of $3.25, and interest rate of 4.75%. Represent the interest rate both as a percentage and as the decimal rate `0.0475`. Do not initialize intended decimal financial values from binary floating-point literals.

---

## Part III — Operators and Expressions

### 1.13.9 Exercise 8 — Account Arithmetic

Given:

```python
account_balance = Decimal("10000.00")
deposit = Decimal("1250.00")
withdrawal = Decimal("450.00")
```

implement an expression that calculates the new account balance. Calculate the expected result manually before running the program and verify the Python result.

### 1.13.10 Exercise 9 — Transaction Value

Given price per unit of $42.75 and quantity of 125, implement `TransactionValue = Price × Quantity`. Calculate and verify the result. Explain whether quantity must always be an integer in real financial applications.

### 1.13.11 Exercise 10 — Percentage Conversion

Convert 5%, 7.25%, 0.50%, 12.75%, and -0.25% into decimal rates using `Decimal`. Explain the relationship `DecimalRate = PercentageRate / 100`.

### 1.13.12 Exercise 11 — Operator Precedence

Consider:

```python
result = principal + principal * rate * years
```

Explain the evaluation order. Rewrite the expression using parentheses so the intended financial structure is visually explicit.

### 1.13.13 Exercise 12 — Mathematical Translation

Translate `A = P(1 + r)^n` into Python using P = 5000, r = 4%, and n = 8 compounding periods. Use `Decimal` where appropriate. Calculate future value before monetary rounding, then round the final result to two decimal places using an explicitly selected rounding mode.

---

## Part IV — Financial Input and Output

### 1.13.14 Exercise 13 — User Input

Implement a program that asks for principal, annual interest rate, and number of years. Convert inputs into appropriate numerical representations and display the interpreted values before performing a financial calculation.

### 1.13.15 Exercise 14 — Percentage Input

Suppose the user enters `6.5` for an annual percentage rate. Implement code that converts it to `0.065`. Explain why using `6.5` directly in a formula expecting a decimal rate would be incorrect.

### 1.13.16 Exercise 15 — Monetary Formatting

Given `amount = Decimal("1234567.8")`, display `$1,234,567.80`. Explain the distinction among calculation, numerical rounding, and display formatting, and why rounding and formatting should not automatically be treated as interchangeable.

---

## Part V — Precision and Rounding

### 1.13.17 Exercise 16 — Floating-Point Observation

Run `print(0.1 + 0.2)`, then compare it with `Decimal("0.1") + Decimal("0.2")`. Explain what the experiment demonstrates about binary floating-point representation and decimal arithmetic.

### 1.13.18 Exercise 17 — Comparing Rounding Modes

Using a quantum of `Decimal("0.01")`, test `Decimal("125.665")` and `Decimal("125.675")` with `ROUND_HALF_UP` and `ROUND_HALF_EVEN`. Compare the results. Explain why `125.665` distinguishes the modes, why `125.675` produces the same result under both, and why a financial application must specify its required rounding rule.

### 1.13.19 Exercise 18 — Centralized Rounding Policy

Define:

```python
MONEY_QUANTUM = Decimal("0.01")
ROUNDING_MODE = ROUND_HALF_UP
```

Implement `round_money(value)` and test `12.345`, `99.999`, `1000.005`, and `0.004`. Record and verify each result. Explain why centralizing monetary rounding can improve consistency.

### 1.13.20 Exercise 19 — Rounding Stage

Compare final-only rounding with rounding and posting after every interest period. Explain why the policies can produce different ending balances and why the governing financial specification—not programmer preference—determines the appropriate rounding stage.

---

## Part VI — Simple Interest

### 1.13.21 Exercise 20 — Simple Interest

Using `I = Prt`, calculate interest and ending balance for P = $12,500, r = 4.5%, and t = 3 years. Implement using `Decimal` and manually verify the result.

### 1.13.22 Exercise 21 — Zero Interest

Use principal = $10,000, rate = 0%, and time = 5 years. Calculate interest and ending balance. Explain why a zero interest rate is a useful boundary test.

### 1.13.23 Exercise 22 — Fractional Time

Use principal = $8,000, annual rate = 6%, and time = 1.5 years. Calculate simple interest and explain whether `years` must be an integer. Treat `1.5 years` as an explicitly supplied model input. Real instruments may derive year fractions using contractual day-count conventions, which are outside Chapter 1 scope.

---

## Part VII — Compound Interest

### 1.13.24 Exercise 23 — Annual Compounding

Using `FV = PV(1 + r)^n`, calculate future value for present value = $15,000, annual rate = 5%, time = 10 years, annual compounding, and n = 10 compounding periods. Use `Decimal` and explicitly state the final rounding policy.

### 1.13.25 Exercise 24 — Monthly Compounding

Use `FV = PV(1 + r/m)^(mt)` with present value = $15,000, annual rate = 5%, 12 compounding periods per year, and 10 years. Compare the result with annual compounding and explain the difference under the model assumptions.

### 1.13.26 Exercise 25 — Compounding Validation

Explain the mathematical problem created when `periods_per_year = 0` and implement validation preventing it. These exercises also assume rates for which the periodic growth factor `1 + r/m` is meaningful under the stated model; complete domain validation depends on the financial specification.

---

## Part VIII — Percentage Change and Returns

### 1.13.27 Exercise 26 — Price Increase

A security price changes from $80 to $92. Calculate the percentage change and verify it using Python.

### 1.13.28 Exercise 27 — Price Decrease

A security price changes from $120 to $102. Calculate the percentage change and explain the sign of the result.

### 1.13.29 Exercise 28 — Zero Starting Value

Given old value = 0 and new value = 50, explain why the standard percentage-change formula cannot be evaluated and implement validation before division.

### 1.13.30 Exercise 29 — Negative Starting Value

Given old value = -100 and new value = -50, evaluate the standard percentage-change expression mathematically. Discuss why its financial interpretation can depend on the domain, distinguishing mathematical computability from financial interpretation.

---

## Part IX — Investment Profit and Loss

For Exercises 30–33, use a simplified model excluding fees, taxes, dividends, financing costs, bid-ask spreads, corporate actions, and timing effects. Preserve unrounded computational values until the applicable numerical policy requires rounding.

### 1.13.31 Exercise 30 — Investment Profit

An investor purchases 250 units at $32.50 and sells them at $37.80. Calculate initial investment, raw profit/loss, display-rounded profit/loss, and percentage return. Calculate percentage return from the unrounded computational profit/loss rather than a value rounded only for presentation.

### 1.13.32 Exercise 31 — Investment Loss

Use purchase price = $75, selling price = $68, and quantity = 150. Calculate initial investment, raw profit/loss, display-rounded loss, and percentage return. Explain the negative sign.

### 1.13.33 Exercise 32 — Break-Even

Use purchase price = $40, selling price = $40, and quantity = 500. Calculate profit/loss, status, and return percentage. Implement logic classifying the result as `BREAK-EVEN`.

### 1.13.34 Exercise 33 — Fractional Quantity

Use purchase price = $125.40, selling price = $132.10, and quantity = 2.75. Calculate simplified profit/loss and percentage return. Explain why `Decimal` can be useful for fractional quantities.

---

## Part X — Transaction Fees

### 1.13.35 Exercise 34 — Percentage-Based Fee

Use transaction amount = $4,250.00 and fee rate = 1.75%. Calculate raw fee, fee under an explicitly stated monetary rounding policy, and total paid when the fee is charged in addition to the transaction amount. Document the rounding stage.

### 1.13.36 Exercise 35 — Fee Deducted From Proceeds

Using the same values, assume instead that the fee is deducted from transaction proceeds. Calculate `NetProceeds = TransactionAmount - Fee`. Compare the two fee semantics and explain why the correct interpretation must come from the applicable business rule.

---

## Part XI — Reconciliation and Tolerances

Calculate reconciliation differences using actual `Decimal` values. Do not introduce additional rounding before comparison unless the reconciliation policy explicitly requires it.

### 1.13.37 Exercise 36 — Exact Match

Use System A = $750,000.00, System B = $750,000.00, and tolerance = $0.01. Calculate difference and reconciliation status.

### 1.13.38 Exercise 37 — Difference Below Tolerance

Use System A = $1000.000, System B = $1000.006, and tolerance = $0.01. Calculate the exact difference before display formatting and determine whether the values reconcile.

### 1.13.39 Exercise 38 — Difference Exactly at Tolerance

Use System A = $1000.00, System B = $1000.01, and tolerance = $0.01. Evaluate `difference <= tolerance`, determine status, and explain why equality is an important boundary test.

### 1.13.40 Exercise 39 — Difference Above Tolerance

Use System A = $1000.000, System B = $1000.011, and tolerance = $0.01. Determine reconciliation status without rounding before comparison. Explain how premature rounding could change a control decision.

### 1.13.41 Exercise 40 — Invalid Tolerance

Consider tolerance = -0.01. Implement validation appropriate for this reconciliation model and explain why the tolerance magnitude cannot be negative.

---

## Part XII — Input Validation

### 1.13.42 Exercise 41 — Invalid Decimal Input

Under the default decimal context, determine what happens when `Decimal("abc")` is evaluated. Catch the relevant conversion exception and display a readable message. Then investigate how decimal context traps affect malformed-input behavior.

### 1.13.43 Exercise 42 — Non-Finite Values

Test `Decimal("NaN")`, `Decimal("Infinity")`, and `Decimal("-Infinity")`. Use `value.is_finite()` to determine whether each should be accepted by the Chapter 1 calculator. Explain why syntactically valid `Decimal` input is not automatically acceptable financial input.

### 1.13.44 Exercise 43 — Reusable Decimal Input

Implement `read_decimal(prompt)` to request input, strip whitespace, convert to `Decimal`, reject non-finite values, and return the valid result. Test `125.75`, whitespace-surrounded input, `abc`, `NaN`, and `Infinity`.

---

## Part XIII — Debugging Exercises

### 1.13.45 Exercise 44 — Percentage Error

Debug code that uses `rate = Decimal("5")` when the programmer intends 5%. Identify and correct the error.

### 1.13.46 Exercise 45 — Float-to-Decimal Construction

Review `amount = Decimal(0.1)`. Replace it with a construction appropriate when the intended decimal value is exactly `0.1` and explain the numerical difference.

### 1.13.47 Exercise 46 — Division-by-Zero Bug

Review the standard percentage-change expression. Identify the input condition that makes it invalid and implement validation before division.

### 1.13.48 Exercise 47 — Reconciliation Rounding Bug

Explain why rounding `abs(system_a - system_b)` before tolerance comparison can be dangerous. Rewrite the logic so the actual calculated difference is compared unless the governing reconciliation specification explicitly requires prior rounding.

### 1.13.49 Exercise 48 — Hidden Rounding Policy

Review `result = round(amount, 2)`. Explain why it might fail to make a financial application's required decimal quantum and rounding policy sufficiently explicit. This does not mean Python's `round()` is inherently incorrect. Rewrite the example so decimal representation, rounding quantum, and rounding mode are explicit.

---

## Part XIV — Applied Problems

### 1.13.50 Applied Problem 1 — Savings Growth Calculator

Implement a program accepting initial deposit, annual interest rate, years, and compounding periods per year. Use the compound future-value model. Use `Decimal`, reject non-finite inputs, require positive compounding frequency, document the model domain, rounding policy, and rounding stage, and display initial value, future value, and total growth.

### 1.13.51 Applied Problem 2 — Investment Trade Calculator

Implement a simplified trade calculator accepting purchase price, selling price, quantity, and fee rate. Calculate purchase value, sale value, purchase fee, sale fee, raw net profit/loss, display-rounded net profit/loss, and return percentage. Explicitly define fee semantics and calculate derived percentages from appropriate computational values rather than display-rounded intermediates.

### 1.13.52 Applied Problem 3 — Account Reconciliation Checker

Implement a reconciliation program accepting System A balance, System B balance, and tolerance. Reject non-finite input and negative tolerance, calculate the actual `Decimal` difference, compare before display rounding, and treat equality with tolerance as within tolerance. Test exact match, below, at, and above tolerance.

### 1.13.53 Applied Problem 4 — Financial Scenario Comparison

Compare $10,000 invested for 10 years under annual rates of 4%, 5%, and 6%, assuming annual compounding. Display scenario, starting value, annual rate, future value, and growth. Explain the results only within the assumptions of the model.

---

## Part XV — Integrated Challenges

### 1.13.54 Challenge 1 — Extend the Chapter Calculator

Extend the Section 1.12 calculator with Present Value, Discount Amount, and Basis-Point Conversion. Before implementation, define each formula, inputs, mathematical validation, domain assumptions, numerical representation, rounding policy, and rounding stage.

### 1.13.55 Challenge 2 — Separate Calculation From Presentation

Select one calculation from Section 1.12 and refactor it conceptually into input, validation, calculation, required rounding, and presentation. Explain the benefits for readability, testing, maintainability, and numerical-policy visibility.

### 1.13.56 Challenge 3 — Design a Test Matrix

Create a test matrix considering normal cases, zero boundaries, meaningful negative values, large values, fractional values, invalid text, non-finite values, rounding boundaries, mathematical boundaries, and business-rule boundaries. For each test record operation, inputs, expected behavior, and reason.

---

## Part XVI — Chapter 1 Capstone

### 1.13.57 Financial Calculation Console

Build a command-line application supporting at least Simple Interest, Compound Interest, Percentage Change, Investment Profit/Loss, Transaction Fee, and Reconciliation.

Requirements include descriptive variable names, `Decimal` for decimal financial quantities, no initialization of intended exact decimals from binary floating-point literals, rejection of non-finite input, correct percentage conversion, denominator validation, compounding validation, nonnegative reconciliation tolerance, explicit monetary rounding policy and stage, preservation of computational values until required rounding, clear formatting, documented assumptions, manual verification, and boundary testing.

Demonstrate at least one successful calculation, one zero-boundary calculation, one invalid-input case, one rounding-boundary case, and one reconciliation-boundary case. For each test record inputs, expected result, actual result, and Pass/Fail status.

---

## Part XVII — Reflection

### 1.13.58 Reflection Questions

Answer the following:

1. Why is translating a financial formula into Python more than simply typing the mathematical expression?
2. Why can numerical representation affect results?
3. What is the difference between decimal representation and decimal context precision?
4. Why should rounding policy be explicit?
5. Why does rounding stage matter?
6. Why are formatting and calculation different concerns?
7. Why should denominators be validated before division?
8. Why can a numerically valid value still violate a financial business rule?
9. Why should reconciliation logic avoid undocumented rounding?
10. Why are boundary tests particularly valuable in financial applications?
11. What assumptions are made by the financial models used in Chapter 1?
12. Why should computational values generally be preserved before display rounding?
13. Which parts of the Basic Financial Calculator would you redesign after learning more advanced Python?

---

## 1.13.59 Chapter 1 Competency Check

After completing Chapter 1, you should be able to explain and demonstrate Python execution basics, variables and data types, financial data representation, operators, expressions and precedence, financial input and output, decimal arithmetic, precision and rounding, validation, basic financial calculations, testing, and verification.

You should also understand the progression:

```text
Python-valid
     │
     ▼
Numerically valid
     │
     ▼
Mathematically valid
     │
     ▼
Financially meaningful
```

A Python program can execute successfully while implementing a financially inappropriate rule. Reliable financial programming therefore requires both computational correctness and understanding of the financial model.

---

## 1.13.60 Chapter 1 Completion Perspective

Chapter 1 began with the question: **What is financial mathematics, and how can Python help us work with it?**

It concludes with the ability to implement, inspect, test, and reason about small financial calculations.

The central Chapter 1 principle is:

> Reliable financial computation does not begin with code. It begins with a clearly defined financial problem and mathematical model.

Python then provides a mechanism for representing, evaluating, validating, testing, and automating that model.

---

## Authoritative References

Technical behavior used by these exercises should be validated primarily against the official Python documentation, including:

- Python `decimal` documentation — `Decimal`, decimal construction, contexts, precision, `quantize()`, rounding modes, `is_finite()`, signals, traps, and `InvalidOperation`.
- Python Built-in Functions documentation — `input()`, `int()`, `round()`, and related built-in behavior.
- Python formatting documentation — formatted string literals and the Format Specification Mini-Language.

The financial formulas are simplified instructional models. Production calculations must additionally be validated against the contractual, accounting, market, institutional, tax, regulatory, or other authoritative requirements governing the specific financial application.

Worked solutions should remain separate from the primary exercise manuscript so the exercises retain their instructional value.

---

## Canonical Workflow Gate

**Section:** 1.13 — Exercises and Applied Problems  
**Draft:** COMPLETE  
**Technical + Source Review:** PASS WITH TECHNICAL REVISIONS  
**Production + Copyright Review:** PASS WITH EDITORIAL REVISIONS  
**Technical revisions incorporated:** YES  
**Production revisions incorporated:** YES  
**Copyright/originality review:** PASS  
**Publication blockers:** NONE  
**Canonical status:** COMPLETE

### Final Disposition

**1.13 — REVISED FINAL / CANONICAL EDITION — APPROVED**

---

## Chapter 1 Completion Gate

With Sections **1.1 through 1.13** canonical, the complete locked Chapter 1 sequence is complete.

**Chapter 1 — Introduction to Python for Financial Mathematics: CANONICAL / COMPLETE**
